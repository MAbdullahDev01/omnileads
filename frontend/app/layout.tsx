import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: 'OmniLeads: The Autonomous CRM & Sales Intelligence Hub',
  description: "This project is a B2B SaaS platform where sales teams manage leads, but with a massive twist: the system predicts which leads will convert using traditional Machine Learning, and employs a team of autonomous AI agents to research prospects and draft highly personalized, mathematically optimized sales pitches using your company's proprietary data.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
